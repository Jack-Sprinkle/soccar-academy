import './MMRTracker.scss';

function MMRTracker({userId, mmr, initialMMR}) {
    console.log(userId, mmr, initialMMR)
    return (
        <div>
            MMR Tracker here
        </div>
    );
};

export default MMRTracker;